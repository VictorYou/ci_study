apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-ldap"
  namespace: {{ .Release.Namespace }}
  labels:
    app: ldap
spec:
  ports:
  - name: openldap
    port: 389
    targetPort: 389
  - name: ldap-admin
    port: 443
    targetPort: 443
  selector:
    app: ldap
